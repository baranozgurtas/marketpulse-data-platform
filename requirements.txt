# MarketPulse Data Platform - Local Dependencies
# NOTE: Airflow and its Snowflake provider are NOT installed locally.
# They run inside the Docker image (docker/Dockerfile.airflow), because
# Airflow 2.8 requires Python <3.12 and local Python is usually newer.

# Ingestion / Snowflake
snowflake-connector-python
requests

# Transformation
dbt-core
dbt-snowflake

# Processing (local Spark runs only)
pyspark==3.5.0

# Dashboard
streamlit
plotly
pandas

# Testing
pytest
